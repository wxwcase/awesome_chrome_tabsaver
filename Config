package.AwesomeChromeTabSaver = {
    interfaces = (1.0);

    build-system = npm-pretty-much;
    build-tools = {
        1.0 = {
            NpmPrettyMuch = 1.10;
            NodeJS = default;
        };
    };

    dependencies = {
        1.0 = {
            # Declare brazil packages that build package.json dependencies here
        };
    };

    test-dependencies = {
        1.0 = {
            # Declare brazil packages that build package.json devDependencies here
        };
    };
};
